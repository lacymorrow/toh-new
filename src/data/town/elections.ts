import type { TownElection } from "./types";

// Elections — to be populated with verified election data.
// Previous entries contained AI-fabricated candidate bios and unverified dates.
export const elections: TownElection[] = [];
